[package]
name = "sxr"
version = "0.1.0"
edition = "2024"

[dependencies]
ab_glyph = "0.2.32"
anyhow = "1.0.104"
eframe = "0.36.1"
image = "0.25.10"
libc = "0.2.189"
tiny-skia = "0.12.0"
wl-clipboard-rs = "0.9.3"
# already in Cargo.lock, pulled in by wl-clipboard-rs: used for the
# zwlr_layer_shell_v1 overlay that replaces the external region selector
wayland-client = "0.31.15"
wayland-protocols-wlr = { version = "0.3.12", features = ["client"] }
